name: flutter_bloc_pattern
description: A library that connects Widgets to BLoC
version: 1.1.0
homepage: https://github.com/hoc081098/flutter_bloc_pattern

environment:
  sdk: '>=2.6.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  rxdart: ^0.23.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^4.0.0
